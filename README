=== Social Wiz ====
启动服务

cd resque   #启动Redis数据库
1. rake redis:start

cd social-web
2. 启动web服务
  rails s
3. 启动scheduler
  rake resque:scheduler
4. 启动队列执行work
  rake resque:work RAILS_ENV=development VERBOSE=1 QUEUES=*


==================================
